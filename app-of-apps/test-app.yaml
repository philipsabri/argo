apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: test-app
  namespace: argocd
spec:
  project: default
  source:
    chart: test-app-chart
    repoURL: oci://ghcr.io/philipsabri
    targetRevision: ">=0.0.0"
    helm:
      releaseName: test-app
  destination:
    server: https://kubernetes.default.svc
    namespace: default
